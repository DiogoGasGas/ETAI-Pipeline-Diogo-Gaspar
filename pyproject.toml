[project]
name = "etai-pipeline-diogo-gaspar"
version = "0.1.0"
description = "Baseline predictive pipeline for the ETAI semester project"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=1.24",
    "pandas>=2.0",
    "pyyaml>=6.0",
    "scikit-learn>=1.3",
]
